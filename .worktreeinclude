# Files Claude Code should copy into new worktrees in addition to tracked files.
# Without this, gitignored secrets (private/.env, etc.) wouldn't reach the
# isolated worktree and every API call would fail with "key not set".
#
# Syntax: same as .gitignore — one path or glob per line.
# See docs/WORKTREES.md for the full workflow.

# Secrets + agent scripts (gitignored — repo is public)
private/
local-cycle.sh
