**/output/
**/__pycache__/
**/*.pyc
**/.env
**/.adk/
.venv/
.article_cache/
*.egg-info/
dist/
.DS_Store
.next/
.playwright-mcp/
temp/

# Private — infrastructure (not for public repo)
# Note: `web/` was un-ignored 2026-05-11 — Next.js source is now tracked.
# Build/runtime artifacts (node_modules, .next, out) stay ignored below.
infra/
docs/ROADMAP.md
private/
shadow_eval/
.claude/
client-content-agent/   # white-label business docs (pricing, strategy, client info) — keep private

# Web build/runtime artifacts (the source is now tracked)
web/node_modules/
web/.next/
web/out/
web/dev-screenshots/

# Personal runner script — gitignored intentionally
local-cycle.sh
