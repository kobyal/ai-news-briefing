# concepts/ — product ideas, not pipeline code

Explorations that aren't part of the daily briefing run.

| Dir | What it is |
|-----|-----------|
| `client-content-agent` | White-label content service concept — productizing the pipeline into paid, on-brand posts for customers. Design/business/demo docs only (`AGENT-DESIGN.md`, `BUSINESS.md`, `DEMO.md`). Not wired into `run_all` or `local-cycle.sh`. |
